#ifndef YAGA_DEMO_SRC_SCENE
#define YAGA_DEMO_SRC_SCENE

#include <memory>

namespace yaga {
namespace demo {

/*
class Scene
{
public:
  virtual void loop() = 0;

  ObjectPtr object;
  ObjectPtr frame;
  ObjectPtr cameraObject;
  Camera*   camera;
  assets::Material* material;
  assets::Material* frameMaterial;
  assets::CameraPtr cameraAsset;
  assets::TransformPtr cameraTransform;
  Scene() : camera(nullptr), material(nullptr), frameMaterial(nullptr) {}
};
*/

} // !namespace demo
} // !namespace yaga

#endif // !YAGA_DEMO_SRC_SCENE
